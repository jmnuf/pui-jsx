{
  "name": "pui-jsx",
  "version": "0.1.0",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "author": "jmnuf <jose.m.nufio.0@gmail.com>",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "bun test",
    "pub": "bun run test && bun run build && npm publish"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
